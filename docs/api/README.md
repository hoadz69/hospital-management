# API Notes

API contracts are placeholders in this scaffold.

Future work:

- Add OpenAPI per service.
- Add gateway routing contracts.
- Add shared event contracts under `backend/shared/contracts`.
- Document tenant context headers and JWT claims.
